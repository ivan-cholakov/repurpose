# Dependencies and build output (rebuilt inside the image)
node_modules
.next
tsconfig.tsbuildinfo

# Secrets and local state — never bake into an image
.env
.env.*
*.db

# VCS / CI
.git
.github
.husky

# Tests and reports (not needed to build or run the app)
e2e
test-results
playwright-report
playwright.config.ts

# Docs and meta
README.md
LAUNCH.md
AGENTS.md
CLAUDE.md

# Docker itself
Dockerfile
.dockerignore
